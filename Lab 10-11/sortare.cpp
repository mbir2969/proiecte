#include "domeniu.h"
#include "sortare.h"


/*void Sortare::sortare(vector<Produs>& sortate, functie_comparare fc)
{
	Produs p{ "default","default",1,"default" };
	for (int i=0;i<size(sortate)-1;i++)
		for ( int j=i+1;j<size(sortate);j++)
			if (fc(sortate.at(i), sortate.at(j)) == false)
			{
				p = sortate.at(i);
				sortate.at(i) = sortate.at(j);
				sortate.at(j) = p;
			}
}*/

