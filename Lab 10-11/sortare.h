#pragma once

#include <vector>
#include "domeniu.h"

using std::vector;

/*Functie de comparare*/
//typedef bool (*functie_comparare)(const Produs& produs1, const Produs& produs2) ;

class Sortare {
public:
	/*Functie care sorteaza lista dupa in functie de functia de comparare data
	* Preconditii: sortate - lista de produse care va fi sortata
	*              fc - functie de comparare
	* Postconditii: sortate - lista de produse sortata
	*/
	//	void sortare(vector<Produs>& sortate, functie_comparare fc);
};